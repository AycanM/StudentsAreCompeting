<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>Parola Değiştirme Ekranı</h2>

            </div>
            <div class="body">

                <form id="form_advanced_validation" method="POST">
                    <div class="form-group form-float">
                        <div class="form-line">
                            <input id="OldPassword" type="password" class="form-control pass" name="minmaxlength1" maxlength="20" minlength="6" required="" aria-required="true">
                            <label class="form-label">Mevcut Parolanız</label>
                        </div>
                        <div class="help-info">Min. 6, Maks. 20 karakter</div>
                    </div>
                    <div class="form-group form-float">
                        <div class="form-line">
                            <input id="NewPassword" type="password" class="form-control pass" name="minmaxlength2" maxlength="20" minlength="6" required="" aria-required="true">
                            <label class="form-label">Yeni Parola</label>
                        </div>
                        <div class="help-info">Min. 6, Maks. 20 karakter</div>
                    </div>
                    <div class="form-group form-float">
                        <div class="form-line">
                            <input id="NewPasswordCnfrm" type="password" class="form-control pass" name="minmaxlength3" maxlength="20" minlength="6" required="" aria-required="true">
                            <label class="form-label">Yeni Parola Onay</label>
                        </div>
                    </div>
                    <button class="btn btn-primary waves-effect" type="submit">KAYDET</button>
                </form>
                <div class="alert alert-success m-t-10">
                    <strong>Başarılı!</strong> Parolanız başarılı bir şekilde değiştirildi.
                </div>
                <div class="alert alert-danger m-t-10" id="PassErr">
                    <strong>Hata!</strong> Parola değiştirme işlemi bir sepebten ötürü başarısız oldu.
                </div>
                <div class="alert alert-danger m-t-10" id="NotInDb">
                    <strong>Hata!</strong> Parola değiştirme işlemi bir sepebten ötürü başarısız oldu.
                </div>
                <div class="alert alert-danger m-t-10" id="NotMatched">
                    <strong>Hata!</strong> Yeni parolanız ile onaylama alanı değeri birbiriyle eşleşmiyor.
                </div>
                <div class="alert alert-danger m-t-10" id="NotChange">
                    <strong>Hata!</strong> Yeni parolanız ile değiştirmek istediğiniz parola aynı olamaz.
                </div>
            </div>
        </div>
    </div>
</div>
